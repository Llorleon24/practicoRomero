<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mapa de Monumentos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
    <style>
        #map { height: 500px; }
        .sidebar {
            max-height: 500px;
            overflow-y: auto;
        }
        .monumento-item {
            cursor: pointer;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }
        .monumento-item:hover {
            background-color: #f8f9fa;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="row">
            <!-- Lista de monumentos -->
            <div class="col-md-4 sidebar">
                <h3 class="text-center">Monumentos</h3>
                <ul id="lista-monumentos" class="list-group"></ul>
            </div>
            <!-- Mapa -->
            <div class="col-md-8">
                <div id="map"></div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        let map = L.map('map').setView([36.7213, -4.4212], 13); // Coordenadas de Málaga
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '&copy; OpenStreetMap contributors'
        }).addTo(map);

        // Cargar GeoJSON
        fetch('/data/monumentos')
            .then(response => response.json())
            .then(data => {
                console.log("Datos cargados:", data); // 🔍 Para verificar estructura en la consola

                data.features.forEach(monumento => {
                    let coords = monumento.geometry.coordinates;
                    let nombre = monumento.properties.NOMBRE || "Sin nombre";
                    let descripcion = monumento.properties.DESCRIPCION || "Sin descripción";

                    // Agregar marcador en el mapa
                    let marker = L.marker([coords[1], coords[0]], {
                        icon: L.icon({
                            iconUrl: '/images/location.png',
                            iconSize: [30, 30]
                        })
                    }).addTo(map).bindPopup(`<h5>${nombre}</h5><p>${descripcion}</p>`);

                    // Agregar a la lista de monumentos
                    let item = document.createElement("li");
                    item.className = "list-group-item monumento-item";
                    item.innerText = nombre;
                    item.onclick = () => {
                        map.setView([coords[1], coords[0]], 15);
                        marker.openPopup();
                    };
                    document.getElementById("lista-monumentos").appendChild(item);
                });
            })
            .catch(error => {
                console.error("Error cargando los datos:", error);
            });
    </script>
</body>
</html>
